# Copy to .env and fill in. Never commit .env.

# hermes: "mock" runs the built-in fake, "live" calls the real endpoint.
HERMES_MODE=mock
HERMES_ENDPOINT=
HERMES_API_KEY=

# publisher: "mock" writes to the local log, "x" posts to X.
PUBLISHER=mock

# images: "mock" writes a placeholder PNG, "openai" calls the image model.
# Only lanes with allow_images in the content YAML ever get one, and Hermes
# decides whether a given post warrants one at all.
IMAGE_RENDERER=mock
OPENAI_API_KEY=

# X API v2, OAuth 1.0a user context. Required only when PUBLISHER=x.
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Approval UI. Leave both blank to run with no auth (local only).
UI_USERNAME=
UI_PASSWORD=

DB_PATH=data/social.db
CONFIG_PATH=config/content.yaml
